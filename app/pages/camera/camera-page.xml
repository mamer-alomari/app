<?xml version="1.0" encoding="utf-8"?>
<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo">
    <GridLayout rows="auto, *, auto">
        <GridLayout columns="auto, *" class="p-4">
            <Label text="‹" tap="{{ onBack }}" class="text-2xl" col="0" />
            <Label text="Add Items" class="text-2xl font-bold text-center" col="1" />
        </GridLayout>

        <StackLayout row="1" class="p-4 justify-center">
            <Button text="Take Photo" 
                    tap="{{ onTakePhoto }}" 
                    class="bg-black text-white text-lg p-4 m-4 rounded-lg" />
            
            <Button text="Upload Image" 
                    tap="{{ onUploadImage }}" 
                    class="bg-white text-black text-lg p-4 m-4 rounded-lg border border-black" />
        </StackLayout>
    </GridLayout>
</Page>